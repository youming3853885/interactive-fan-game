# 節奏畫圈對決模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把遊戲從「亂數各自出方向 + 能量先滿者勝」改成「兩人共用依 BPM 生成的正/反/休段落表、混合計分+Combo、限時比分」的節奏畫圈對決，並加進場門檻（雙方手被鏡頭抓到才播音樂起跑）與長度模式（2分鐘/完整曲）。

**Architecture:** 新增兩個純函數模組 `chart.js`（段落表生成/查詢）與 `score.js`（計分+combo），完全可離線單測。`game.js` 拿掉舊能量邏輯只留風機指令。`ui.js`/`main.js` 改成段落驅動的顯示與計分、回合計時、結算比分。`select-screen.js` 加長度切換與時長顯示、波形置底加強。

**Tech Stack:** 原生 JS + vitest（既有）、既有 `motion.js` 的 `direction`。

**本 repo 的 `.git` 在家目錄 `C:/Users/DCT/.git`；所有 `git add` 一律用明確檔案路徑，絕不用 `git add -A`。**

---

## 檔案結構

| 檔案 | 職責 | 可單測 |
|---|---|---|
| `src/chart.js`（新） | `chartFromBpm` 生成段落表、`segmentAt` 查當前/下一段 | ✅ |
| `src/score.js`（新） | `targetOmegaFor`、`comboMultiplier`、`scoreStep`、`higherScore` | ✅ |
| `src/game.js` | 拿掉能量/亂數邏輯，只留 `fanCommand` + 精簡 `CONFIG` | ✅ |
| `src/ui.js` | 段落大字+combo+倒數下一段+剩餘時間+雙方分數；導引亮點速度＝目標轉速 | 手動 |
| `src/select-screen.js` | 長度切換(2分鐘/完整曲)+時長顯示；波形置底不重疊、加強 | 部分(`formatTime`) |
| `src/main.js` | 進場門檻→播音樂起跑；回合計時；段落驅動計分；結算比分 | 手動 |

---

### Task 1: chart.js — 段落表生成與查詢（TDD）

**Files:**
- Create: `src/chart.js`
- Test: `src/chart.test.js`

- [ ] **Step 1: 寫失敗測試**

`src/chart.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { chartFromBpm, segmentAt } from './chart.js';

describe('chartFromBpm', () => {
  it('120BPM/3星/24秒 → 每段8拍(4秒)、正反休循環、涵蓋整段', () => {
    const c = chartFromBpm(120, 3, 24);
    expect(c[0]).toEqual({ dir: 'F', startSec: 0, endSec: 4 });
    expect(c[1]).toEqual({ dir: 'R', startSec: 4, endSec: 8 });
    expect(c[2]).toEqual({ dir: 'S', startSec: 8, endSec: 12 });
    expect(c[3].dir).toBe('F');
    expect(c[c.length - 1].endSec).toBe(24); // 裁切到 roundSec
  });

  it('高難度(≥4星)每段4拍、樣式休息更少', () => {
    const c = chartFromBpm(120, 5, 100);
    expect(c[0].endSec).toBe(2);               // 4拍*0.5s = 2s
    expect(c.slice(0, 5).map((s) => s.dir)).toEqual(['F', 'R', 'F', 'R', 'S']);
  });

  it('最後一段裁切、不超過 roundSec', () => {
    const c = chartFromBpm(120, 3, 10); // 段長4s → 0-4,4-8,8-10(裁切)
    expect(c[c.length - 1]).toEqual({ dir: 'S', startSec: 8, endSec: 10 });
  });
});

describe('segmentAt', () => {
  const c = chartFromBpm(120, 3, 24);
  it('回傳當前段、下一段、當前剩餘秒數', () => {
    const r = segmentAt(c, 5);
    expect(r.current.dir).toBe('R');
    expect(r.next.dir).toBe('S');
    expect(r.remain).toBeCloseTo(3);
  });
  it('超過結尾 → current 為 null', () => {
    expect(segmentAt(c, 999).current).toBe(null);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm test -- chart`
Expected: FAIL，`Failed to resolve import "./chart.js"`。

- [ ] **Step 3: 寫最小實作**

`src/chart.js`:
```js
// 依 BPM 決定性生成段落表（正F/反R/休S），對齊節拍。
export function chartFromBpm(bpm, stars, roundSec) {
  const beatSec = 60 / bpm;
  const segBeats = stars >= 4 ? 4 : 8;
  const pattern = stars >= 4 ? ['F', 'R', 'F', 'R', 'S'] : ['F', 'R', 'S'];
  const segLen = segBeats * beatSec;
  const chart = [];
  let t = 0, i = 0;
  while (t < roundSec) {
    const end = Math.min(t + segLen, roundSec);
    chart.push({ dir: pattern[i % pattern.length], startSec: t, endSec: end });
    t = end; i++;
  }
  return chart;
}

// 查 t 秒時的當前段/下一段/當前剩餘秒。超過結尾回 current:null。
export function segmentAt(chart, t) {
  for (let i = 0; i < chart.length; i++) {
    if (t < chart[i].endSec) {
      return { current: chart[i], next: chart[i + 1] || null, remain: chart[i].endSec - t };
    }
  }
  return { current: null, next: null, remain: 0 };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm test -- chart`
Expected: PASS（5 個測試）。

- [ ] **Step 5: Commit**

```bash
git add src/chart.js src/chart.test.js
git commit -m "feat(chart): 依BPM生成正反休段落表 + 查詢"
```

---

### Task 2: score.js — 計分與 Combo（TDD）

**Files:**
- Create: `src/score.js`
- Test: `src/score.test.js`

- [ ] **Step 1: 寫失敗測試**

`src/score.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { SCORE_CFG, targetOmegaFor, comboMultiplier, scoreStep, higherScore } from './score.js';

describe('targetOmegaFor', () => {
  it('每2拍一圈：120BPM → 2π rad/s', () => {
    expect(targetOmegaFor(120, SCORE_CFG)).toBeCloseTo(2 * Math.PI);
  });
});

describe('comboMultiplier', () => {
  it('每 comboStep 秒 +1，封頂 comboMax', () => {
    expect(comboMultiplier(0, SCORE_CFG)).toBe(1);
    expect(comboMultiplier(SCORE_CFG.comboStep * 2, SCORE_CFG)).toBe(3);
    expect(comboMultiplier(9999, SCORE_CFG)).toBe(SCORE_CFG.comboMax);
  });
});

describe('scoreStep', () => {
  it('休息段：保留 combo、不加分', () => {
    expect(scoreStep({ score: 50, combo: 2 }, 'S', 6, 0.1, 120, SCORE_CFG))
      .toEqual({ score: 50, combo: 2 });
  });
  it('做對且合拍：加分、combo 隨時間升', () => {
    const r = scoreStep({ score: 0, combo: 0 }, 'F', 2 * Math.PI, 0.1, 120, SCORE_CFG);
    expect(r.combo).toBeCloseTo(0.1);
    // baseRate*dt*tempoBonus*comboMult(=1) = 100*0.1*1.6*1 = 16
    expect(r.score).toBeCloseTo(16);
  });
  it('方向錯 → combo 歸零、不加分', () => {
    expect(scoreStep({ score: 50, combo: 2 }, 'F', -6, 0.1, 120, SCORE_CFG))
      .toEqual({ score: 50, combo: 0 });
  });
  it('做對但不合拍(轉太快) → 只給基礎分(無加成)', () => {
    const r = scoreStep({ score: 0, combo: 0 }, 'F', 50, 0.1, 120, SCORE_CFG);
    expect(r.score).toBeCloseTo(10); // 100*0.1*1*1
  });
});

describe('higherScore', () => {
  it('比分高者勝、平手 null', () => {
    expect(higherScore(100, 50)).toBe('A');
    expect(higherScore(20, 80)).toBe('B');
    expect(higherScore(30, 30)).toBe(null);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm test -- score`
Expected: FAIL，`Failed to resolve import "./score.js"`。

- [ ] **Step 3: 寫最小實作**

`src/score.js`:
```js
import { direction } from './motion.js';

export const SCORE_CFG = {
  deadzone: 1.5,       // rad/s，低於此視為停手
  baseRate: 100,       // 每秒基礎分
  tempoBonus: 1.6,     // 合拍加成倍率
  tempoLo: 0.6,        // 合拍窗下界（× 目標轉速）
  tempoHi: 1.6,        // 合拍窗上界
  comboStep: 3,        // 每持續做對幾秒 +1 倍
  comboMax: 5,         // combo 倍率上限
  revsPerBeat: 0.5,    // 目標：每 2 拍一圈
  scoreForFullBar: 3000, // 分數條/LED 滿格對應分數
};

export function targetOmegaFor(bpm, cfg) {
  const beatSec = 60 / bpm;
  return cfg.revsPerBeat * 2 * Math.PI / beatSec;
}

export function comboMultiplier(combo, cfg) {
  return Math.min(cfg.comboMax, 1 + Math.floor(combo / cfg.comboStep));
}

// state = { score, combo(秒) }；segDir ∈ 'F'|'R'|'S'。回傳新 state。
export function scoreStep(state, segDir, omega, dt, bpm, cfg) {
  let { score, combo } = state;
  if (segDir === 'S' || segDir == null) return { score, combo }; // 休息：保留 combo
  const dir = direction(omega, cfg.deadzone);
  const correct = dir !== 'S' && dir === segDir;
  if (!correct) return { score, combo: 0 };
  const target = targetOmegaFor(bpm, cfg);
  const mag = Math.abs(omega);
  const onTempo = mag >= cfg.tempoLo * target && mag <= cfg.tempoHi * target;
  const tempoMult = onTempo ? cfg.tempoBonus : 1;
  combo += dt;
  score += cfg.baseRate * dt * tempoMult * comboMultiplier(combo, cfg);
  return { score, combo };
}

export function higherScore(a, b) {
  if (a > b) return 'A';
  if (b > a) return 'B';
  return null;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm test -- score`
Expected: PASS（8 個測試）。

- [ ] **Step 5: Commit**

```bash
git add src/score.js src/score.test.js
git commit -m "feat(score): 混合計分+Combo+合拍加成 純函數"
```

---

### Task 3: game.js — 精簡為只留風機指令（TDD）

**Files:**
- Modify: `src/game.js`
- Modify: `src/game.test.js`

- [ ] **Step 1: 改寫 game.test.js（移除能量/勝負測試，只留風機）**

`src/game.test.js`（整檔取代）:
```js
import { describe, it, expect } from 'vitest';
import { CONFIG, fanCommand } from './game.js';

describe('fanCommand', () => {
  it('停手 → S / pwm 0', () => {
    expect(fanCommand(0, CONFIG)).toEqual({ dir: 'S', pwm: 0 });
  });
  it('滿速正轉 → F / pwm 255', () => {
    expect(fanCommand(CONFIG.omegaMax, CONFIG)).toEqual({ dir: 'F', pwm: 255 });
  });
  it('滿速反轉 → R / pwm 255', () => {
    expect(fanCommand(-CONFIG.omegaMax, CONFIG)).toEqual({ dir: 'R', pwm: 255 });
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm test -- game`
Expected: FAIL（舊 game.js 仍匯出 updateChannel 等；但此步的目的是先讓測試檔對齊新介面。實際上舊 fanCommand 測試會過、匯入的 CONFIG 仍在，可能直接 PASS。若 PASS 就直接進 Step 3 精簡實作再跑一次確認仍 PASS）。

- [ ] **Step 3: 精簡 game.js（整檔取代）**

`src/game.js`:
```js
import { direction } from './motion.js';

// 風機相關可調常數（校正旋鈕）
export const CONFIG = {
  deadzone: 1.5,  // rad/s，低於此視為停手
  omegaMax: 12,   // rad/s，對應風機滿速（約 2 圈/秒）
  pwmMin: 80,     // DC 馬達死區下限，低於此轉不動
};

// 風機永遠跟玩家真實手勢方向/轉速。
export function fanCommand(omega, cfg) {
  const dir = direction(omega, cfg.deadzone);
  if (dir === 'S') return { dir: 'S', pwm: 0 };
  const mag = Math.min(Math.abs(omega), cfg.omegaMax);
  const pwm = Math.round(cfg.pwmMin + (mag / cfg.omegaMax) * (255 - cfg.pwmMin));
  return { dir, pwm };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm test -- game`
Expected: PASS（3 個測試）。

- [ ] **Step 5: Commit**

```bash
git add src/game.js src/game.test.js
git commit -m "refactor(game): 精簡為只留風機指令，能量邏輯移交 score/chart"
```

---

### Task 4: ui.js — 段落顯示 + 分數 + combo + 導引亮點速度

**Files:**
- Modify: `src/ui.js`

（畫布繪製，無法單測；用 Task 6 在瀏覽器手動驗證。此步改 `render`/`victory` 與導引速度。）

- [ ] **Step 1: 改 render 與 victory（用下列新版取代 return 物件內的 render 與 victory）**

在 `src/ui.js` 的 `createUI` 內，把 `return { ... }` 內的 `render(state)` 與 `victory(who)` 換成：
```js
    // state = { timeLeft, segDir, nextDir, nextIn, guideOmega,
    //           A:{score, comboMult, hand, shoulder}, B:{...} }
    render(state) {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      const W = canvas.width, H = canvas.height;
      drawDivider();

      // 目前段落大字 + 倒數下一段 + 剩餘時間
      const glyph = state.segDir === 'F' ? '↻ 正轉' : state.segDir === 'R' ? '↺ 反轉' : '⏸ 休息';
      centerText(glyph, 0.12, 0.08, '#fff');
      if (state.nextDir) {
        const nn = state.nextDir === 'F' ? '正轉' : state.nextDir === 'R' ? '反轉' : '休息';
        centerText(`下一個：${nn}  ${Math.ceil(state.nextIn)}`, 0.22, 0.03, '#cdd6ff');
      }
      centerText(`⏱ ${Math.ceil(state.timeLeft)}s`, 0.05, 0.03, '#fff');

      // 分數條（＝分數進度）+ combo
      const frac = (v) => Math.max(0, Math.min(1, v / 3000)); // scoreForFullBar
      drawBar(0.06, frac(state.A.score) * 100, colorA, `A  ${Math.round(state.A.score)}`);
      drawBar(0.94, frac(state.B.score) * 100, colorB, `B  ${Math.round(state.B.score)}`);
      centerTextAt(0.2, 0.9, `x${state.A.comboMult}`, 0.045, colorA);
      centerTextAt(0.8, 0.9, `x${state.B.comboMult}`, 0.045, colorB);

      // 手上噴粒子
      for (const [side, color] of [['A', colorA], ['B', colorB]]) {
        const h = state[side].hand;
        if (h) spawnParticles(h.x, h.y, color);
      }
      for (let i = particles.length - 1; i >= 0; i--) {
        const p = particles[i];
        p.x += p.vx; p.y += p.vy; p.life -= 0.04;
        if (p.life <= 0) { particles.splice(i, 1); continue; }
        ctx.globalAlpha = p.life; ctx.fillStyle = p.color;
        ctx.beginPath(); ctx.arc(p.x, p.y, 5, 0, Math.PI * 2); ctx.fill();
      }
      ctx.globalAlpha = 1;

      // 導引圓 + 亮點（速度＝目標轉速），只在活躍段顯示
      guidePhase = (guidePhase + (state.guideOmega || 0) / 60) % (Math.PI * 2);
      const R = Math.min(W, H) * 0.16;
      for (const [side, color] of [['A', colorA], ['B', colorB]]) {
        const s = state[side];
        const center = s.shoulder || { x: (side === 'A' ? 0.25 : 0.75) * W, y: H * 0.5 };
        if (state.segDir !== 'S') drawGuide(center, R, state.segDir, color);
        drawHand(s.hand, color);
      }
    },

    victory(who, scoreA, scoreB) {
      ctx.fillStyle = '#000a';
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = who === 'A' ? colorA : who === 'B' ? colorB : '#fff';
      ctx.font = `${Math.round(canvas.height * 0.1)}px system-ui`;
      ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
      const title = who ? `玩家 ${who} 勝利！` : '平手！';
      ctx.fillText(title, canvas.width / 2, canvas.height * 0.42);
      ctx.font = `${Math.round(canvas.height * 0.05)}px system-ui`;
      ctx.fillStyle = '#fff';
      ctx.fillText(`A ${Math.round(scoreA)}  :  ${Math.round(scoreB)} B`, canvas.width / 2, canvas.height * 0.56);
    },
```

- [ ] **Step 2: 加 drawGuide 的方向參數改用 state.segDir（已相容）並新增 centerTextAt 輔助**

在 `src/ui.js` 的 `centerText` 函式後面新增：
```js
  function centerTextAt(xFrac, yFrac, text, sizeFrac, color) {
    const W = canvas.width, H = canvas.height;
    ctx.fillStyle = color; ctx.font = `bold ${Math.round(H * sizeFrac)}px system-ui`;
    ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
    ctx.fillText(text, W * xFrac, H * yFrac);
  }
```

- [ ] **Step 3: 建置確認無語法錯**

Run: `npm run build`
Expected: `built in ...`，無錯誤。

- [ ] **Step 4: Commit**

```bash
git add src/ui.js
git commit -m "feat(ui): 段落大字+倒數+剩餘時間+雙方分數+combo，導引亮點速度=目標轉速"
```

---

### Task 5: select-screen.js — 長度模式 + 時長顯示 + 波形置底加強

**Files:**
- Modify: `src/select-screen.js`
- Test: `src/select-screen.test.js`（新，只測 `formatTime` 純函數）

- [ ] **Step 1: 寫 formatTime 失敗測試**

`src/select-screen.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { formatTime } from './select-screen.js';

describe('formatTime', () => {
  it('秒 → m:ss', () => {
    expect(formatTime(0)).toBe('0:00');
    expect(formatTime(5)).toBe('0:05');
    expect(formatTime(125)).toBe('2:05');
  });
  it('NaN/未知 → --:--', () => {
    expect(formatTime(NaN)).toBe('--:--');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm test -- select-screen`
Expected: FAIL，`formatTime` 未匯出。

- [ ] **Step 3: 在 select-screen.js 檔案最上方（import 之後）新增並匯出 formatTime**

在 `src/select-screen.js` 頂部 import 區塊之後加入：
```js
export function formatTime(sec) {
  if (!Number.isFinite(sec)) return '--:--';
  const s = Math.floor(sec);
  return `${Math.floor(s / 60)}:${String(s % 60).padStart(2, '0')}`;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm test -- select-screen`
Expected: PASS。

- [ ] **Step 5: 加長度切換 + 時長顯示 UI，並回傳當前選擇**

在 `createSelectScreen` 的 `screen.innerHTML` 中，`.ss-info` 那行後面加一列長度切換與時長（找到 `<div class="ss-info">...</div>` 之後插入）：
```html
    <div class="ss-len">遊戲長度
      <span class="ss-seg"><button data-len="2" class="on">2 分鐘</button><button data-len="F">完整曲</button></span>
      <span class="ss-dur" id="ssDur"></span>
    </div>
```
在注入的 CSS（`injectStyle` 內）加入：
```css
  .ss-len{margin-top:10px;display:flex;gap:10px;justify-content:center;align-items:center;color:#aeb4d8;font-size:14px;}
  .ss-len .ss-seg{display:flex;border:1px solid #ffffff2e;border-radius:999px;overflow:hidden;}
  .ss-len .ss-seg button{background:transparent;color:#cdd6ff;border:none;padding:6px 14px;cursor:pointer;font-size:14px;}
  .ss-len .ss-seg button.on{background:linear-gradient(135deg,#2b7bff,#7aa8ff);color:#0b0b12;font-weight:700;}
  .ss-dur{color:#cdd6ff;}
```
在 `createSelectScreen` 內、`render()` 定義前加入長度狀態與時長探測：
```js
  let lenMode = '2'; // '2'=2分鐘, 'F'=完整曲
  const durProbe = document.createElement('video');
  durProbe.preload = 'metadata';
  const durCache = {};
  function showDuration() {
    const t = tracks[i];
    const d = durCache[t.id];
    $('ssDur').textContent = `🕒 時長 ${formatTime(d)}`;
    if (d === undefined) {
      durProbe.src = t.src;
      durProbe.onloadedmetadata = () => { durCache[t.id] = durProbe.duration; if (tracks[i] === t) $('ssDur').textContent = `🕒 時長 ${formatTime(durProbe.duration)}`; };
    }
  }
```
在 `render()` 結尾呼叫 `showDuration();`，並綁定長度按鈕（在事件綁定區塊加入）：
```js
  screen.querySelectorAll('.ss-len [data-len]').forEach((b) => {
    b.addEventListener('click', () => {
      lenMode = b.dataset.len;
      screen.querySelectorAll('.ss-len [data-len]').forEach((x) => x.classList.toggle('on', x === b));
      sfx.hover();
    });
  });
```
把 `onPick(i)` 兩處呼叫改成帶長度模式：`onPick(i, lenMode)`（`$('ssStart')` click 與 Enter 鍵各一處）。

- [ ] **Step 6: 波形置底加強（改 eqLoop 放大振幅）**

把 eqLoop 內的 `const lit = Math.max(1, Math.round(v * ROWS));` 改為（低值拉高、波動更強烈）：
```js
        const lit = Math.max(1, Math.round(Math.pow(v, 0.7) * ROWS));
```
並把 `.ss-eq` 的 `height:18vh` 改成 `height:13vh`（波形帶更矮、上方留白給元件；`z-index:0` 與內容 `z-index:1` 已有，確保不重疊）。

- [ ] **Step 7: 建置確認**

Run: `npm run build`
Expected: 無錯誤。

- [ ] **Step 8: Commit**

```bash
git add src/select-screen.js src/select-screen.test.js
git commit -m "feat(select): 長度模式(2分鐘/完整曲)+整首時長顯示+波形置底加強"
```

---

### Task 6: main.js — 進場門檻起跑 + 回合計時 + 段落計分 + 結算

**Files:**
- Modify: `src/main.js`

- [ ] **Step 1: 改 import 與狀態（頂部）**

把 `src/main.js` 的 game/score 相關 import 與狀態改成：
```js
import { wristAngle, trackRotation } from './motion.js';
import { CONFIG, fanCommand } from './game.js';
import { chartFromBpm, segmentAt } from './chart.js';
import { SCORE_CFG, scoreStep, targetOmegaFor, comboMultiplier, higherScore } from './score.js';
import { formatCommand } from './protocol.js';
import { connectSerial, simSender } from './serial.js';
import { createPoseReader, pickArm } from './pose.js';
import { createUI } from './ui.js';
import { createMusicWidget } from './music.js';
import { BUILTIN_TRACKS, bpmToStars } from './tracks.js';
import { loadSettings } from './settings.js';
import { createSettingsPanel } from './settings-panel.js';
import { createSelectScreen } from './select-screen.js';
import { updateHold } from './ready.js';
import { createLoadingScreen } from './loading.js';
import { preloadTracks } from './preload.js';
```
把舊的 `chA/chB` 能量狀態換成分數狀態與回合變數：
```js
const READY_NEED = 5;
let phase = 'loading';
let media = null;
let selectScreen = null;
let selectedIdx = 0;
let lenMode = '2';
let chart = [];
let bpm = 120;
let roundSec = 120;
let startTime = 0;
let scoreA = { score: 0, combo: 0 };
let scoreB = { score: 0, combo: 0 };
const rotA = { lastAngle: null };
const rotB = { lastAngle: null };
const readyState = { need: READY_NEED, A: { hold: 0, ready: false }, B: { hold: 0, ready: false } };
let ended = false;
let last = performance.now();
```

- [ ] **Step 2: 改 onPick、startReady、startPlaying、sendStop**

把選歌與狀態轉換改成：
```js
function startReady() {
  readyState.A = { hold: 0, ready: false };
  readyState.B = { hold: 0, ready: false };
  phase = 'ready';
}

function startPlaying() {
  const t = media.tracks[selectedIdx];
  bpm = t.bpm || 120;
  const songLen = Number.isFinite(mvVideo.duration) ? mvVideo.duration : 120;
  roundSec = lenMode === '2' ? Math.min(120, songLen) : songLen;
  chart = chartFromBpm(bpm, bpmToStars(bpm), roundSec);
  scoreA = { score: 0, combo: 0 };
  scoreB = { score: 0, combo: 0 };
  ended = false;
  media.playTrack(selectedIdx);        // ← 就緒後才播音樂
  startTime = performance.now();        // 段落表 t=0 對齊音樂起點
  last = startTime;
  phase = 'playing';
  // 音樂 metadata 晚到時，修正 roundSec
  mvVideo.addEventListener('loadedmetadata', () => {
    const sl = mvVideo.duration;
    if (Number.isFinite(sl)) { roundSec = lenMode === '2' ? Math.min(120, sl) : sl; chart = chartFromBpm(bpm, bpmToStars(bpm), roundSec); }
  }, { once: true });
}

function sendStop() {
  sender.send(formatCommand({ dir: 'S', pwm: 0, energy: 0 }, { dir: 'S', pwm: 0, energy: 0 })).catch(() => {});
}
```
並把 `createSelectScreen` 的 onPick 改成接收長度模式：
```js
  selectScreen = createSelectScreen(hud, (idx, mode) => {
    selectedIdx = idx;
    lenMode = mode;
    selectScreen.hide();
    startReady();
  });
```

- [ ] **Step 3: 改 loop 的 playing 分支（段落驅動計分 + 結算）**

把 `loop` 內 `phase === 'playing'` 分支換成：
```js
  } else if (phase === 'playing') {
    const elapsed = (performance.now() - startTime) / 1000;
    const timeLeft = Math.max(0, roundSec - elapsed);
    const { current, next, remain } = segmentAt(chart, elapsed);
    const segDir = current ? current.dir : 'S';

    scoreA = scoreStep(scoreA, segDir, omegaA, dt, bpm, SCORE_CFG);
    scoreB = scoreStep(scoreB, segDir, omegaB, dt, bpm, SCORE_CFG);

    const fa = fanCommand(omegaA, CONFIG);
    const fb = fanCommand(omegaB, CONFIG);
    sender.send(formatCommand(
      { ...fa, energy: Math.min(100, (scoreA.score / SCORE_CFG.scoreForFullBar) * 100) },
      { ...fb, energy: Math.min(100, (scoreB.score / SCORE_CFG.scoreForFullBar) * 100) },
    )).catch(() => {});

    ui.render({
      timeLeft,
      segDir,
      nextDir: next ? next.dir : null,
      nextIn: remain,
      guideOmega: targetOmegaFor(bpm, SCORE_CFG),
      A: { score: scoreA.score, comboMult: comboMultiplier(scoreA.combo, SCORE_CFG), hand: handA, shoulder: armA ? toCanvas(armA.shoulder, 'A') : null },
      B: { score: scoreB.score, comboMult: comboMultiplier(scoreB.combo, SCORE_CFG), hand: handB, shoulder: armB ? toCanvas(armB.shoulder, 'B') : null },
    });

    if (!ended && elapsed >= roundSec) {
      ended = true;
      phase = 'victory';
      sendStop();
      const who = higherScore(scoreA.score, scoreB.score);
      ui.victory(who, scoreA.score, scoreB.score);
      setTimeout(() => { selectScreen.show(media.tracks); phase = 'select'; }, 4000);
    }
  }
```
（`select`/`ready` 分支維持原樣：`select` 清畫面送停、`ready` 畫方塊+5秒門檻，就緒呼叫 `startPlaying()`。移除舊的 `chA/chB/updateChannel/winner/CONFIG.winAt` 相關殘留。）

- [ ] **Step 4: 手動驗證（sim 模式，免 Arduino）**

Run: `npm run dev`，Chrome 開 `http://localhost:5173`，允許攝影機。
Expected 流程：
- LOADING → 選歌（可切 2分鐘/完整曲、看到時長）→ 選歌後**音樂還沒響**。
- 準備階段：兩人揮手→方塊→各撐 5 秒 → **音樂開始播、遊戲開跑**。
- 中央大字輪流 ↻正轉/↺反轉/⏸休息（跟著 BPM 切換），旁邊倒數下一段；做對方向且合拍時分數上升、combo 倍率上升；方向錯 combo 歸零；休息段不加分。
- 上方剩餘時間倒數；時間到（2分鐘或整首）→ 顯示「玩家 X 勝利！ A:B 分數」，4 秒回選歌。
- 左藍右紅、手掌圖示、導引圓亮點以目標轉速繞行。

- [ ] **Step 5: Commit**

```bash
git add src/main.js
git commit -m "feat(main): 就緒才起跑+回合計時+段落驅動計分+結算比分"
```

---

## Self-Review 檢查結果

**Spec 覆蓋：**
- 進場門檻（就緒才播音樂起跑）→ Task 6 `startReady`/`startPlaying`（`media.playTrack` 移到就緒後）。
- 共用段落表依 BPM 生成 → Task 1 `chart.js`（Task 6 用同一 `chart` 給雙方）。
- 混合計分+合拍加成+combo、休息保留 combo → Task 2 `score.js`。
- 段落大字+倒數下一段+剩餘時間+雙方分數+combo、導引亮點速度=目標轉速 → Task 4 `ui.js`。
- 限時比分（2分鐘/整首）、時長顯示 → Task 5（時長/長度）+ Task 6（roundSec/結算 `higherScore`）。
- 波形置底不重疊、加強 → Task 5 Step 6。
- 風機跟手勢、LED=分數條 → Task 3 `fanCommand` + Task 6 送 energy=分數比例。
- 左藍右紅、手掌圖示 → 既有（前次已做，Task 4 沿用 colorA/colorB 與 drawHand）。

**型別一致：** `chartFromBpm/segmentAt`、`scoreStep(state,segDir,omega,dt,bpm,cfg)`、`comboMultiplier`、`targetOmegaFor`、`higherScore`、`fanCommand`、`ui.render(state{timeLeft,segDir,nextDir,nextIn,guideOmega,A,B})`、`ui.victory(who,scoreA,scoreB)`、`onPick(idx,mode)` 於各 Task 定義與使用一致。

**佔位符：** 無 TBD/TODO；每個程式步驟含完整程式碼。可調數值（BPM 段長、combo、目標轉速、scoreForFullBar）標為校正旋鈕。
