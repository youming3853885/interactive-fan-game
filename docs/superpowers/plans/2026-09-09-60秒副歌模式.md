# 60 秒副歌模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓玩家在選歌時多一個「60秒副歌」時長選項，播放預先切好的副歌/高潮片段、限時 60 秒。

**Architecture:** 全部離線一次性預處理：偵測腳本（numpy/scipy + ffmpeg，不進 build）找出每首副歌起點 → ffmpeg 切 6 支 `public/mv/{id}-chorus.mp4`。遊戲端只加一個純函數 `pickPlayback(track, lenMode)` 決定載哪支片與 roundSec，並在選歌畫面加一顆按鈕。

**Tech Stack:** Python 3.13（numpy/scipy 已裝）、ffmpeg/ffprobe、Vite + vanilla JS、vitest。

**參考 spec：** `docs/superpowers/specs/2026-09-09-60秒副歌模式-design.md`

**慣例雷區（CLAUDE.md）：** 絕不 `git add -A`（家目錄有 .git），一律明確路徑 add；除手🖐外遊戲無 emoji；資產路徑用 `import.meta.env.BASE_URL`。

---

## 檔案結構

| 檔案 | 責任 | 動作 |
|---|---|---|
| `tools/detect-chorus.py` | 離線：偵測 6 首副歌起點 → 寫 `tools/chorus-times.json` | 建立（dev-only） |
| `tools/cut-chorus.py` | 離線：讀 json → ffmpeg 切 6 支 `-chorus.mp4` | 建立（dev-only） |
| `tools/chorus-times.json` | 偵測結果（人眼複核/手調） | 產出 |
| `public/mv/{id}-chorus.mp4` | 6 支 60 秒副歌短片 | 產出 |
| `src/tracks.js` | 加 `chorusSrc` 欄位 + 純函數 `pickPlayback` | 修改 |
| `src/tracks.test.js` | `pickPlayback` 單元測試 | 修改 |
| `src/music.js` | `loadTrack/prep/playTrack` 接受 `lenMode`、用 `pickPlayback` 選 src | 修改 |
| `src/main.js` | `startPlaying` 用 `pickPlayback` 決定 src/roundSec；傳 `lenMode` 給 media | 修改 |
| `src/select-screen.js` | 時長選項加「60秒副歌」按鈕 | 修改 |

`detect`/`cut` 是開發工具、**靠人眼複核**，無自動測試。`pickPlayback` 是純函數、**有單元測試**。media/main/select-screen 是轉接層、靠 `npm run build` + 實機驗證（符合 CLAUDE.md）。

---

### Task 1: 離線偵測腳本 → 產出 chorus-times.json

**Files:**
- Create: `tools/detect-chorus.py`
- Produce: `tools/chorus-times.json`

- [ ] **Step 1: 寫偵測腳本**

建立 `tools/detect-chorus.py`：

```python
#!/usr/bin/env python3
# 離線偵測 6 首副歌/高潮起點（秒），寫入 tools/chorus-times.json 供人眼複核。
# 只用 numpy/scipy + ffmpeg，不裝 librosa/pychorus。從專案根目錄執行。
import subprocess, json, numpy as np

# (track_id, 來源檔名, 方法)：流行用 chroma 重複偵測、國樂用 energy 高潮偵測
SONGS = [
    ('canon-rock',   'canon-rock.mp4',          'chroma'),
    ('super-run',    'super-run.mp4',           'chroma'),
    ('initial-d',    'initial-d.mp4',           'chroma'),
    ('bumblebee',    'flight-of-bumblebee.mp4', 'chroma'),
    ('dragon-boat',  'dragon-boat.mp4',         'energy'),
    ('golden-snake', 'golden-snake.mp4',        'energy'),
]
MV = 'public/mv'
SR = 22050

def decode(path):
    # ffmpeg 解碼成單聲道 16-bit PCM，讀進 numpy [-1,1]
    cmd = ['ffmpeg', '-v', 'error', '-i', path, '-ac', '1', '-ar', str(SR), '-f', 's16le', '-']
    raw = subprocess.run(cmd, capture_output=True).stdout
    return np.frombuffer(raw, np.int16).astype(np.float32) / 32768.0

def energy_start(x, win=60.0, hop=0.5):
    # 滑動 60 秒視窗，回傳「平均能量最高」視窗的起點秒數（B 法）
    fl = int(hop * SR)
    n = len(x) // fl
    rms = np.sqrt(np.array([np.mean(x[i*fl:(i+1)*fl]**2) for i in range(n)]) + 1e-9)
    W = int(win / hop)
    if n <= W:
        return 0.0
    csum = np.cumsum(np.insert(rms, 0, 0))
    wmean = (csum[W:] - csum[:-W]) / W
    return float(np.argmax(wmean)) * hop

def chroma(x, hop=0.25):
    fl = int(hop * SR); win = fl * 2
    n = max(0, (len(x) - win) // fl)
    freqs = np.fft.rfftfreq(win, 1 / SR)
    pc = np.full(len(freqs), -1)
    valid = freqs > 20
    pc[valid] = (np.round(12 * np.log2(freqs[valid] / 440.0) + 69).astype(int)) % 12
    w = np.hanning(win)
    C = np.zeros((n, 12))
    for i in range(n):
        mag = np.abs(np.fft.rfft(x[i*fl:i*fl+win] * w))
        for k in range(12):
            C[i, k] = mag[pc == k].sum()
    C /= (np.linalg.norm(C, axis=1, keepdims=True) + 1e-9)
    return C, hop

def chorus_start(x):
    # chroma 自相似 time-lag：找「最常重複的 ~15 秒段落」起點（A 法）
    C, hop = chroma(x)
    n = len(C)
    seg = int(15 / hop)          # 重複段長 ~15 秒
    min_lag = int(10 / hop)      # 兩次出現至少相隔 10 秒
    if n <= seg + min_lag:
        return 0.0
    S = C @ C.T                  # 已正規化 → cosine 相似度
    best_val, best_i = -1.0, 0
    for lag in range(min_lag, n - seg):
        idx = np.arange(0, n - lag)
        diag = S[idx, idx + lag]
        if len(diag) < seg:
            continue
        csum = np.cumsum(np.insert(diag, 0, 0))
        block = csum[seg:] - csum[:-seg]
        j = int(np.argmax(block))
        if block[j] > best_val:
            best_val, best_i = float(block[j]), j
    return best_i * hop

def main():
    out = {}
    for tid, fname, method in SONGS:
        x = decode(f'{MV}/{fname}')
        start = energy_start(x) if method == 'energy' else chorus_start(x)
        out[tid] = round(start, 1)
        print(f'{tid:14s} {method:7s} start={out[tid]}s  (len={len(x)/SR:.0f}s)')
    json.dump(out, open('tools/chorus-times.json', 'w'), indent=2)
    print('→ tools/chorus-times.json 已寫入，請人眼複核')

if __name__ == '__main__':
    main()
```

- [ ] **Step 2: 執行偵測**

Run（從專案根目錄）: `python tools/detect-chorus.py`
Expected: 印出 6 行 `id method start=Xs`，並產生 `tools/chorus-times.json`（6 個 `id: 秒數`）。若某首 ffmpeg 讀取失敗會是空 stdout → 檢查檔名與路徑。

- [ ] **Step 3: 人眼複核（暫不切片）**

打開 `tools/chorus-times.json` 看 6 個秒數是否合理（副歌通常不在 0 秒、也不在最後 10 秒內）。此時**先不切**，Task 2 切完再逐首聽。先 commit 腳本與初版 json。

- [ ] **Step 4: Commit**

```bash
git add tools/detect-chorus.py tools/chorus-times.json
git commit -m "feat(副歌): 離線副歌偵測腳本(chroma重複/energy高潮)+初版時間點"
```

---

### Task 2: 切 6 支副歌短片並人眼複核

**Files:**
- Create: `tools/cut-chorus.py`
- Produce: `public/mv/{id}-chorus.mp4` ×6

- [ ] **Step 1: 寫切片腳本**

建立 `tools/cut-chorus.py`：

```python
#!/usr/bin/env python3
# 讀 tools/chorus-times.json，對每首往前含 2.5 秒前奏切 60 秒 → public/mv/{id}-chorus.mp4。
# 邊界：太靠結尾就把起點前挪讓片段收在曲末；整片<60s 用整片。從專案根目錄執行。
import subprocess, json

SRC = {  # track_id → 來源檔名（與 detect-chorus.py 一致）
    'canon-rock':   'canon-rock.mp4',
    'super-run':    'super-run.mp4',
    'initial-d':    'initial-d.mp4',
    'bumblebee':    'flight-of-bumblebee.mp4',
    'dragon-boat':  'dragon-boat.mp4',
    'golden-snake': 'golden-snake.mp4',
}
MV = 'public/mv'
LEAD = 2.5   # 前奏秒數
DUR = 60.0   # 目標片長

def probe_dur(path):
    out = subprocess.run(
        ['ffprobe', '-v', 'error', '-show_entries', 'format=duration',
         '-of', 'default=noprint_wrappers=1:nokey=1', path],
        capture_output=True, text=True).stdout.strip()
    return float(out)

def main():
    times = json.load(open('tools/chorus-times.json'))
    for tid, chorus in times.items():
        src = f'{MV}/{SRC[tid]}'
        dur = probe_dur(src)
        length = min(DUR, dur)
        start = max(0.0, min(chorus - LEAD, max(0.0, dur - length)))
        dst = f'{MV}/{tid}-chorus.mp4'
        subprocess.run([
            'ffmpeg', '-y', '-ss', f'{start:.2f}', '-i', src, '-t', f'{length:.2f}',
            '-c:v', 'libx264', '-preset', 'veryfast', '-crf', '23',
            '-c:a', 'aac', '-b:a', '128k', '-movflags', '+faststart', dst,
        ], check=True)
        print(f'{tid:14s} start={start:.1f}s len={length:.1f}s → {dst}')

if __name__ == '__main__':
    main()
```

- [ ] **Step 2: 切片**

Run: `python tools/cut-chorus.py`
Expected: 印出 6 行，`public/mv/` 出現 6 支 `{id}-chorus.mp4`（每支約 3–6 MB）。

- [ ] **Step 3: 人眼/耳複核每支**

逐支確認命中副歌/高潮且從頭乾淨起播：
```bash
ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 public/mv/canon-rock-chorus.mp4
```
每支長度應 ≈ 60（短曲可較短）。用系統播放器或 `start public/mv/<id>-chorus.mp4` 聽開頭 5 秒。
**若某首抓歪**：手動改 `tools/chorus-times.json` 該首秒數 → 重跑 `python tools/cut-chorus.py`（只需再看那首）。反覆到 6 支都滿意。

- [ ] **Step 4: Commit**

```bash
git add tools/cut-chorus.py tools/chorus-times.json public/mv/canon-rock-chorus.mp4 public/mv/super-run-chorus.mp4 public/mv/initial-d-chorus.mp4 public/mv/bumblebee-chorus.mp4 public/mv/dragon-boat-chorus.mp4 public/mv/golden-snake-chorus.mp4
git commit -m "feat(副歌): 切出6支60秒副歌短片(人眼複核命中)"
```

---

### Task 3: `pickPlayback` 純函數 + `chorusSrc` 欄位（TDD）

**Files:**
- Modify: `src/tracks.js`
- Test: `src/tracks.test.js`

- [ ] **Step 1: 寫失敗測試**

在 `src/tracks.test.js` 頂部 import 補上 `pickPlayback`，並新增 describe：

```javascript
import { bpmToStars, starString, pickPlayback, BUILTIN_TRACKS } from './tracks.js';

describe('pickPlayback（時長模式 → 播放來源/局長）', () => {
  const t = { id: 'canon-rock', src: 'blob:full', chorusSrc: '/base/mv/canon-rock-chorus.mp4' };
  it('chorus 模式 → 載副歌短片、roundSec 固定 60', () => {
    expect(pickPlayback(t, 'chorus')).toEqual({ src: '/base/mv/canon-rock-chorus.mp4', roundSec: 60 });
  });
  it('2 分鐘模式 → 完整 src、roundSec 交給 duration(null)', () => {
    expect(pickPlayback(t, '2')).toEqual({ src: 'blob:full', roundSec: null });
  });
  it('完整曲模式 → 完整 src、roundSec 交給 duration(null)', () => {
    expect(pickPlayback(t, 'F')).toEqual({ src: 'blob:full', roundSec: null });
  });
  it('每首內建曲都有 chorusSrc（{id}-chorus.mp4）', () => {
    for (const bt of BUILTIN_TRACKS) expect(bt.chorusSrc).toContain(`${bt.id}-chorus.mp4`);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npx vitest run src/tracks.test.js`
Expected: FAIL — `pickPlayback is not a function` / `chorusSrc` undefined。

- [ ] **Step 3: 實作**

在 `src/tracks.js` 的 `BUILTIN_TRACKS` 陣列**定義之後**加一行補 `chorusSrc`，並在檔尾加 `pickPlayback`：

```javascript
// 每首推導副歌短片路徑（Task 2 產出的 public/mv/{id}-chorus.mp4）
for (const t of BUILTIN_TRACKS) t.chorusSrc = `${base}mv/${t.id}-chorus.mp4`;

// 選歌時長模式 → 播放來源與本局長度。
// roundSec 為 null 表示「開播後由 MV loadedmetadata 的 duration 決定」（沿用現行邏輯）。
export function pickPlayback(track, lenMode) {
  if (lenMode === 'chorus') return { src: track.chorusSrc, roundSec: 60 };
  return { src: track.src, roundSec: null };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npx vitest run src/tracks.test.js`
Expected: PASS（含既有 bpmToStars 測試）。

- [ ] **Step 5: Commit**

```bash
git add src/tracks.js src/tracks.test.js
git commit -m "feat(副歌): pickPlayback純函數+chorusSrc欄位(TDD)"
```

---

### Task 4: `music.js` 依 lenMode 選播放來源

**Files:**
- Modify: `src/music.js`

- [ ] **Step 1: 改 loadTrack/prep/playTrack 接受 lenMode**

`src/music.js` 頂部 import 補 `pickPlayback`；`loadTrack` 用它選 src；`prep`/`playTrack` 多收 `lenMode` 轉傳：

```javascript
import { defaultTrackSetting } from './settings.js';
import { pickPlayback } from './tracks.js';
```

把 `loadTrack` 改成：

```javascript
  function loadTrack(i, autoplay, lenMode) {
    idx = i;
    const t = tracks[idx];
    const cfg = trackSetting(t);
    videoEl.src = pickPlayback(t, lenMode).src; // chorus 模式載 -chorus.mp4，否則完整 MV
    videoEl.volume = cfg.volume / 100;
    videoEl.style.opacity = String(cfg.mvOpacity / 100);
    if (autoplay) videoEl.play().catch(() => {});
  }
```

把回傳物件的 `prep`/`playTrack` 改成：

```javascript
    prep(i, lenMode) { loadTrack(i, false, lenMode); }, // 只載入緩衝、不自動播
    playTrack(i, lenMode) { loadTrack(i, true, lenMode); },
```

（`loadTrack(0, false)` 初始預載那行不用改：lenMode 為 undefined → pickPlayback 回完整 src。）

- [ ] **Step 2: build 驗證**

Run: `npm run build`
Expected: `✓ built`，零錯誤。

- [ ] **Step 3: Commit**

```bash
git add src/music.js
git commit -m "feat(副歌): music.js 依 lenMode 選播放來源(pickPlayback)"
```

---

### Task 5: `main.js` startPlaying 套用 chorus 模式

**Files:**
- Modify: `src/main.js`（import 區、`startPlaying` 約 114–137、`media.prep` 呼叫約 172）

- [ ] **Step 1: import 補 pickPlayback**

找到 main.js 從 `./tracks.js` 匯入的那行（含 `bpmToStars`），把 `pickPlayback` 加進去，例如：

```javascript
import { BUILTIN_TRACKS, bpmToStars, pickPlayback } from './tracks.js';
```

（實際具名清單以檔案現況為準，只需把 `pickPlayback` 併入同一個 from './tracks.js' 的 import。）

- [ ] **Step 2: 改 startPlaying 用 pickPlayback 決定 src/roundSec**

把 `startPlaying` 開頭與結尾的 loadedmetadata handler 改成：

```javascript
function startPlaying() {
  const t = media.tracks[selectedIdx];
  bpm = t.bpm || 120;
  const pb = pickPlayback(t, lenMode);                 // { src, roundSec }
  const songLen = Number.isFinite(mvVideo.duration) ? mvVideo.duration : 120;
  roundSec = pb.roundSec != null ? pb.roundSec         // chorus 固定 60
    : (lenMode === '2' ? Math.min(120, songLen) : songLen);
  chart = chartFromBpm(bpm, bpmToStars(bpm), roundSec);
  maxScore = estimateMaxScore(roundSec, bpm);
  scoreA = newScore();
  scoreB = newScore();
  ended = false;
  media.playTrack(selectedIdx, lenMode);               // ← 傳 lenMode，chorus 會載短片
  if (!mvAnalyser) { try { mvAnalyser = attachAnalyser(mvVideo); mvFreq = new Uint8Array(mvAnalyser.frequencyBinCount); } catch { mvAnalyser = null; } }
  scoreS = newScore();
  rotA.lastAngle = null; rotB.lastAngle = null; rotS.lastAngle = null;
  video.style.opacity = '0';
  startTime = performance.now();
  last = startTime;
  lastCountSec = -1; victoryResult = null;
  phase = 'playing';
  mvVideo.addEventListener('loadedmetadata', () => {
    if (pb.roundSec != null) return;                   // chorus：固定 60，不用 duration 覆蓋
    const sl = mvVideo.duration;
    if (Number.isFinite(sl)) { roundSec = lenMode === '2' ? Math.min(120, sl) : sl; chart = chartFromBpm(bpm, bpmToStars(bpm), roundSec); }
  }, { once: true });
}
```

- [ ] **Step 3: prep 呼叫也傳 lenMode**

找到 `media.prep(selectedIdx);`（modeModal callback 內，約 172 行）改成：

```javascript
    media.prep(selectedIdx, lenMode); // 用 ready 期間預載選中曲（chorus 模式載短片）
```

- [ ] **Step 4: build 驗證**

Run: `npm run build`
Expected: `✓ built`，零錯誤。

- [ ] **Step 5: Commit**

```bash
git add src/main.js
git commit -m "feat(副歌): main.js startPlaying 套用 chorus 模式(60秒/載短片)"
```

---

### Task 6: 選歌畫面加「60秒副歌」按鈕

**Files:**
- Modify: `src/select-screen.js:36`

- [ ] **Step 1: 加按鈕**

把 `.ss-seg` 那段（約 36 行）改成三顆，「60秒副歌」放最前、預設仍在「2 分鐘」：

```html
        <span class="ss-seg"><button data-len="chorus">60秒副歌</button><button data-len="2" class="on">2 分鐘</button><button data-len="F">完整曲</button></span>
```

（`lenMode` 預設 `'2'`、選擇/高亮邏輯 `.ss-len [data-len]` 已泛用，`data-len="chorus"` 會自動套用，無需改 JS。）

- [ ] **Step 2: build 驗證**

Run: `npm run build`
Expected: `✓ built`，零錯誤。

- [ ] **Step 3: Commit**

```bash
git add src/select-screen.js
git commit -m "feat(副歌): 選歌畫面加60秒副歌時長選項"
```

---

### Task 7: 全量驗證 + 實機試玩

**Files:** 無（驗證）

- [ ] **Step 1: 全測試 + build**

Run: `npm test && npm run build`
Expected: 測試全綠（含新的 pickPlayback）、`✓ built` 零錯誤，`dist/mv/` 含 6 支 `-chorus.mp4`。

- [ ] **Step 2: 實機試玩（`npm run dev`，Chrome/Edge，允許攝影機）**

逐項確認：
1. 選歌畫面出現三顆時長按鈕「60秒副歌 / 2分鐘 / 完整曲」，預設 2 分鐘高亮。
2. 選「60秒副歌」→ 開始 → 播放的是副歌短片、從頭起播、約 60 秒後結算。
3. 切「2分鐘 / 完整曲」→ 行為與原本一致（回歸驗證）。
4. 至少測 1 首國樂（金蛇狂舞）+ 1 首流行（Canon Rock）的副歌片，確認命中且畫圈計分正常。

- [ ] **Step 3: 收尾**

全部通過即完成。若要上線：push 到 `main` 觸發 GitHub Pages 部署（依慣例明確路徑、勿 `git add -A`）。

---

## Self-Review

**Spec coverage：**
- 離線偵測 A/B（numpy/scipy+ffmpeg，不裝 librosa）→ Task 1 ✅
- 切 60 秒、前奏 2.5s、邊界保護、人眼複核 → Task 2 ✅
- 遊戲唯一改動＝多一個時長選項 + pickPlayback → Task 3–6 ✅
- pickPlayback 純函數 + 單元測試 → Task 3 ✅
- roundSec chorus 固定 60、其餘沿用 duration → Task 3/5（roundSec:null 契約一致）✅
- 副歌短片選到才載（media.prep 在 ready 期間）→ Task 4/5 ✅
- 測試（純函數單測 + 實機三模式）→ Task 3/7 ✅

**Placeholder scan：** 無 TBD/TODO；每個 code step 均含完整程式碼。

**Type/名稱一致性：** `pickPlayback(track, lenMode) → {src, roundSec}` 在 Task 3 定義，Task 4（music.js `.src`）、Task 5（main.js `.roundSec != null`）一致使用；`chorusSrc` 欄位在 Task 3 定義、Task 3 測試引用；副歌檔名 `{id}-chorus.mp4` 在 Task 2 產出、Task 3 `chorusSrc` 推導、Task 7 dist 驗證三處一致；`lenMode` 值 `'chorus'/'2'/'F'` 在 Task 6（按鈕）、Task 3/5（分支）一致。
