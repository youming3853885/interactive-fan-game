# 殘缺版風扇 + 燈條分數進度顯示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** P2 單風扇 20% 整場連轉(雙人勝利再轉10秒)、滿分精算×0.7、燈條改顯示分數進度＋判定閃爍＋倒數紅＋方向反轉。

**Architecture:** 純函數(score/protocol/game)先行＋vitest;main.js 接線;單條版韌體加 J/U 疊加層、REVERSED、移除踢腳、PWM_MAX 提到 51。

**Tech Stack:** Vite + vanilla JS + vitest;Arduino(FastLED)。

Spec: `docs/superpowers/specs/2026-09-10-殘缺版風扇與燈條進度顯示-design.md`

---

### Task 1: score.js 滿分精算

**Files:** Modify `src/score.js`、Test `src/score.test.js`

- [ ] Step 1: score.test.js 加測試(120BPM/24秒: F8s+R8s+S8s → 16圈全PERFECT含combo = 19200;S段不計分)
- [ ] Step 2: 跑測試確認 fail
- [ ] Step 3: score.js 加:

```js
export const BAR_FULL_RATIO = 0.7; // 滿條門檻 = 理論滿分 × 此係數（小學生玩得不錯就能看到接近滿條）

// 依譜面精算理論滿分：只計 F/R 段、每圈 PERFECT、combo 跨段累積
export function maxScoreForChart(chart, bpm, cfg) {
  const revTime = (2 * Math.PI) / targetOmegaFor(bpm, cfg);
  let combo = 0, total = 0;
  for (const seg of chart) {
    if (seg.dir === 'S') continue;
    const revs = Math.floor((seg.endSec - seg.startSec) / revTime);
    for (let r = 0; r < revs; r++) { combo += 1; total += revScore(combo, 'PERFECT', cfg); }
  }
  return total || 1;
}
```

- [ ] Step 4: 測試過
- [ ] Step 5: commit

### Task 2: protocol.js J/U 指令

**Files:** Modify `src/protocol.js`、Test `src/protocol.test.js`

- [ ] Step 1: 測試:`judgeFlashLine('D','PERFECT')==='J,D,1\n'`、GREAT→`J,D,2\n`、GOOD→null;`urgentLine(true)==='U,1\n'`
- [ ] Step 2: fail 確認
- [ ] Step 3: 實作:

```js
// 得分閃爍(疊加層)：PERFECT=1 金閃、GREAT=2 白閃、GOOD 不閃(回 null)
export function judgeFlashLine(target, judgment) {
  const n = judgment === 'PERFECT' ? 1 : judgment === 'GREAT' ? 2 : 0;
  return n ? `J,${target},${n}\n` : null;
}
// 最後倒數紅色模式
export function urgentLine(on) { return `U,${on ? 1 : 0}\n`; }
```

- [ ] Step 4: 測試過
- [ ] Step 5: commit

### Task 3: game.js 殘缺版風扇常數

**Files:** Modify `src/game.js`、`src/game.test.js`

- [ ] Step 1: game.js 改 `power: 0.20`、刪 `fanForSegment`,加:

```js
// 殘缺版：P2 風扇整場固定正轉(不踢腳、不隨休息段起停)
export function fanRun(cfg) { return { dir: 'F', pwm: Math.round(255 * (cfg.power ?? 1)) }; }
```

- [ ] Step 2: game.test.js 改測 `fanRun(CONFIG)` → `{dir:'F',pwm:51}`
- [ ] Step 3: 測試過、commit

### Task 4: main.js 接線

**Files:** Modify `src/main.js`

- [ ] Step 1: import 改 `fanRun`、加 `maxScoreForChart, BAR_FULL_RATIO`(score.js)、`judgeFlashLine, urgentLine`(protocol.js)
- [ ] Step 2: `startPlaying` 用 `maxScore = Math.max(1, Math.round(maxScoreForChart(chart, bpm, SCORE_CFG) * BAR_FULL_RATIO))` 取代 estimateMaxScore(loadedmetadata 重算 chart 處同步重算);刪 estimateMaxScore;`urgentSent=false`
- [ ] Step 3: playing 每幀:A 恆停、B 送 `fanRun(CONFIG)`;energy 改送分數進度 `Math.min(100, st.score / maxScore * 100)`(單人=S、雙人 A/B 各自)
- [ ] Step 4: stepPlayer 判定處:`const fl = judgeFlashLine('D', j); if (fl) sender.send(fl).catch(() => {});`
- [ ] Step 5: playing:`if (timeLeft <= 10 && !urgentSent) { urgentSent = true; sender.send(urgentLine(true)).catch(() => {}); }`;sendStop() 補送 `urgentLine(false)`
- [ ] Step 6: endRound:改送 `motorTestLine('A',0)` + `motorTestLine('B', mode==='dual' ? 20 : 0)` + `effectLine('D', FX.FIREWORK)`(M 指令不動燈,煙火不被蓋;victory 期間本來就不送 A/B 幀指令)
- [ ] Step 7: `npm test` + `npm run build` 全過、commit

### Task 5: 單條版韌體

**Files:** Modify `firmware/interactive_fan_nano_single/interactive_fan_nano_single.ino`

- [ ] Step 1: `PWM_MAX 38→51`(20%)、`TRIM_B 120→100`(20% 為實測值,不再補償)、刪 KICK 兩個 define
- [ ] Step 2: 刪 MotorState/motors/setMotor/motorTick,A/B 遊戲 token 改 `motorGame(i,dir,pwm)`(夾 PWM_MAX 後 motorOut);loop 刪 motorTick 呼叫
- [ ] Step 3: 加 `#define REVERSED 1`;showStrip 輸出前反轉緩衝
- [ ] Step 4: 加 flash 疊加層(`flashUntil/flashColor`,token `J`:1=Gold 2=White,250ms)與 `urgent`(token `U`);FX_ENERGY+urgent → 紅色脈動;loop 的刷新條件納入 flash/urgent
- [ ] Step 5: 逐項核對 spec 行為、commit

### Task 6: 整合驗證上線

- [ ] `npm test` 全過、`npm run build` 零錯誤
- [ ] push main(auto deploy);提醒使用者重燒單條版韌體
