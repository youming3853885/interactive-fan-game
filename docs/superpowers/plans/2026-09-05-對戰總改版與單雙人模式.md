# 對戰總改版 + 單/雙人模式 + 選歌遊戲化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 加入模式選擇彈窗(單/雙人)、單人模式(雙手同計分+評級)、對戰畫面總改版(隱藏攝影機看MV、導引圓固定、combo華麗爆發+音效、遊戲化能量條、8秒段落、停音樂+結算)、選歌遊戲化(霓虹背景+試聽體積光+音波不重疊)、校名品牌。

**Architecture:** 純函數擴充 `score.js`(單人計分+評級)、`chart.js`(8秒段落) 可離線單測。新增 `mode-modal.js`(模式彈窗)、`lights.js`(體積光引擎)。`ui.js`/`main.js`/`select-screen.js` 整合視覺與流程。

**Tech Stack:** 原生 JS + vitest、Canvas、WebAudio、TF.js MoveNet。

**本 repo `.git` 在 `C:/Users/DCT/.git`；所有 `git add` 用明確路徑，絕不 `git add -A`。**

---

### Task 1: score.js — 單人計分 + 評級（TDD）

**Files:** Modify `src/score.js`, `src/score.test.js`

- [ ] **Step 1: 加測試到 `src/score.test.js`（在檔尾 append）**

```js
import { scoreStepSingle, gradeFor } from './score.js';

describe('scoreStepSingle', () => {
  const T = 2 * Math.PI; // 120BPM 目標轉速
  it('兩手都做對且合拍 → 滿速加分', () => {
    const r = scoreStepSingle({ score: 0, combo: 0 }, 'F', T, T, 0.1, 120, SCORE_CFG);
    expect(r.score).toBeCloseTo(16); // 100*0.1*(2/2)*1.6*1
  });
  it('只一手做對 → 半速', () => {
    const r = scoreStepSingle({ score: 0, combo: 0 }, 'F', T, -T, 0.1, 120, SCORE_CFG);
    expect(r.score).toBeCloseTo(8); // *(1/2)
  });
  it('都沒做對 → combo 歸零、不加分', () => {
    expect(scoreStepSingle({ score: 50, combo: 3 }, 'F', -T, -T, 0.1, 120, SCORE_CFG))
      .toEqual({ score: 50, combo: 0 });
  });
  it('休息段 → 保留 combo', () => {
    expect(scoreStepSingle({ score: 50, combo: 3 }, 'S', T, T, 0.1, 120, SCORE_CFG))
      .toEqual({ score: 50, combo: 3 });
  });
});

describe('gradeFor', () => {
  it('依正規化比例給 S/A/B/C', () => {
    const R = 100; // baseRate
    expect(gradeFor(R * 120 * 1.6, 120, SCORE_CFG)).toBe('S');
    expect(gradeFor(R * 120 * 1.0, 120, SCORE_CFG)).toBe('A');
    expect(gradeFor(R * 120 * 0.6, 120, SCORE_CFG)).toBe('B');
    expect(gradeFor(R * 120 * 0.2, 120, SCORE_CFG)).toBe('C');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗** — Run: `npm test -- score` → FAIL（未匯出）。

- [ ] **Step 3: 在 `src/score.js` 加常數與函式**

在 `SCORE_CFG` 物件內加三個門檻（放在 `scoreForFullBar` 後）：
```js
  gradeS: 1.5, gradeA: 1.0, gradeB: 0.5, // 單人評級門檻(× baseRate*roundSec)
```
在檔尾 append：
```js
// 單人模式：兩手同段落方向才滿速；單手半速；都沒做斷 combo。
export function scoreStepSingle(state, segDir, omegaL, omegaR, dt, bpm, cfg) {
  let { score, combo } = state;
  if (segDir === 'S' || segDir == null) return { score, combo };
  const dl = direction(omegaL, cfg.deadzone), dr = direction(omegaR, cfg.deadzone);
  const cL = dl !== 'S' && dl === segDir, cR = dr !== 'S' && dr === segDir;
  const n = (cL ? 1 : 0) + (cR ? 1 : 0);
  if (n === 0) return { score, combo: 0 };
  combo += dt;
  const mag = Math.max(cL ? Math.abs(omegaL) : 0, cR ? Math.abs(omegaR) : 0);
  const target = targetOmegaFor(bpm, cfg);
  const onTempo = mag >= cfg.tempoLo * target && mag <= cfg.tempoHi * target;
  const tempoMult = onTempo ? cfg.tempoBonus : 1;
  score += cfg.baseRate * dt * (n / 2) * tempoMult * comboMultiplier(combo, cfg);
  return { score, combo };
}

// 依分數正規化(除以 baseRate*roundSec)給評級。
export function gradeFor(score, roundSec, cfg) {
  const ratio = score / (cfg.baseRate * roundSec);
  if (ratio >= cfg.gradeS) return 'S';
  if (ratio >= cfg.gradeA) return 'A';
  if (ratio >= cfg.gradeB) return 'B';
  return 'C';
}
```

- [ ] **Step 4: 跑測試** — Run: `npm test -- score` → PASS。

- [ ] **Step 5: Commit**
```bash
git add src/score.js src/score.test.js
git commit -m "feat(score): 單人雙手計分 scoreStepSingle + 評級 gradeFor"
```

---

### Task 2: chart.js — 段落改 ≈8 秒（TDD）

**Files:** Modify `src/chart.js`, `src/chart.test.js`

- [ ] **Step 1: 改 `src/chart.test.js`（取代 chartFromBpm 的三個 it 與 segmentAt 的 const c）**

把 `describe('chartFromBpm'...)` 三個 it 換成：
```js
  it('120BPM/3星 → 每段≈8秒(16拍)、正反休循環', () => {
    const c = chartFromBpm(120, 3, 32);
    expect(c[0]).toEqual({ dir: 'F', startSec: 0, endSec: 8 });
    expect(c[1]).toEqual({ dir: 'R', startSec: 8, endSec: 16 });
    expect(c[2]).toEqual({ dir: 'S', startSec: 16, endSec: 24 });
    expect(c[3].dir).toBe('F');
    expect(c[c.length - 1].endSec).toBe(32);
  });
  it('高難度(≥4星)樣式休息更少', () => {
    const c = chartFromBpm(120, 5, 100);
    expect(c.slice(0, 5).map((s) => s.dir)).toEqual(['F', 'R', 'F', 'R', 'S']);
  });
  it('最後一段裁切、不超過 roundSec', () => {
    const c = chartFromBpm(120, 3, 20); // 段長8s → 0-8,8-16,16-20
    expect(c[c.length - 1]).toEqual({ dir: 'S', startSec: 16, endSec: 20 });
  });
```
把 `describe('segmentAt'...)` 的 `const c = chartFromBpm(120, 3, 24);` 改成 `chartFromBpm(120, 3, 32);`，其 `segmentAt(c, 5)` 的期望改為：
```js
    const r = segmentAt(c, 10);
    expect(r.current.dir).toBe('R');   // 8-16
    expect(r.next.dir).toBe('S');
    expect(r.remain).toBeCloseTo(6);
```
（「超過結尾」的 it 保持 `segmentAt(c, 999).current` === null）

- [ ] **Step 2: 跑測試確認失敗** — Run: `npm test -- chart` → FAIL。

- [ ] **Step 3: 改 `src/chart.js` 的 segBeats**

把 `const segBeats = stars >= 4 ? 4 : 8;` 改成：
```js
  const segBeats = Math.max(4, Math.round(8 / beatSec)); // ≈8秒，對齊節拍
```
（pattern 那行維持依 stars 不變。）

- [ ] **Step 4: 跑測試** — Run: `npm test -- chart` → PASS。

- [ ] **Step 5: Commit**
```bash
git add src/chart.js src/chart.test.js
git commit -m "feat(chart): 段落長度改≈8秒(對齊節拍)"
```

---

### Task 3: sfx.js — combo 爆發音效

**Files:** Modify `src/sfx.js`

- [ ] **Step 1: 在 `sfx` 物件內加方法（在 `confirm` 後）**
```js
  comboBurst(tier) {
    const base = 440 * Math.pow(1.12, tier);
    [0, 80, 160].forEach((d, k) => setTimeout(() => blip(base * (1 + k * 0.25), 0.12, 'square', 0.16), d));
  },
```

- [ ] **Step 2: 建置** — Run: `npm run build` → 無錯。

- [ ] **Step 3: Commit**
```bash
git add src/sfx.js
git commit -m "feat(sfx): combo 爆發上行音效"
```

---

### Task 4: pose.js — 全畫面單人偵測 readFull

**Files:** Modify `src/pose.js`

- [ ] **Step 1: 在 `createPoseReader` 內加全畫面偵測器與 readFull**

在建立 `detA`/`detB` 後加：
```js
  const detFull = await poseDetection.createDetector(model, cfg);
```
在 `return { async read() {...} }` 內加入 `readFull`（與 read 並列）：
```js
    async readFull() {
      const poses = await detFull.estimatePoses(video);
      if (!poses.length) return null;
      const kp = Object.fromEntries(poses[0].keypoints.map((k) => [k.name, k]));
      return {
        leftWrist: kp['left_wrist'], leftShoulder: kp['left_shoulder'],
        rightWrist: kp['right_wrist'], rightShoulder: kp['right_shoulder'],
      };
    },
```

- [ ] **Step 2: 建置** — Run: `npm run build` → 無錯。

- [ ] **Step 3: Commit**
```bash
git add src/pose.js
git commit -m "feat(pose): 單人模式全畫面偵測 readFull"
```

---

### Task 5: loading.js — 標題加校名

**Files:** Modify `src/loading.js`

- [ ] **Step 1: 改標題文字**

把 `<div class="ld-title">畫 圈 對 決</div>` 換成：
```html
    <div class="ld-title" style="font-size:26px;letter-spacing:.06em">澎湖縣湖西鄉龍門國民小學</div>
    <div class="ld-title">畫 圈 對 決</div>
```

- [ ] **Step 2: 建置** — Run: `npm run build` → 無錯。

- [ ] **Step 3: Commit**
```bash
git add src/loading.js
git commit -m "feat(loading): 標題加校名 澎湖縣湖西鄉龍門國民小學"
```

---

### Task 6: mode-modal.js — 模式選擇彈窗（遊戲化）

**Files:** Create `src/mode-modal.js`

- [ ] **Step 1: 建立檔案**
```js
// 開始遊戲後的模式選擇彈窗：霓虹卡片，單人 / 雙人。onPick('single'|'dual')。
export function createModeModal(hud, onPick) {
  const style = document.createElement('style');
  style.textContent = `
  .mm-back{position:absolute;inset:0;display:none;align-items:center;justify-content:center;gap:40px;
    background:#05060dcc;backdrop-filter:blur(4px);z-index:40;pointer-events:auto;}
  .mm-card{width:min(38vw,320px);aspect-ratio:3/4;border-radius:20px;cursor:pointer;color:#fff;
    display:flex;flex-direction:column;align-items:center;justify-content:center;gap:14px;
    background:#0e1430;border:2px solid #ffffff2a;transition:.18s;font-family:system-ui,sans-serif;}
  .mm-card:hover{transform:translateY(-6px) scale(1.04);}
  .mm-card .ic{font-size:88px;} .mm-card .t{font-size:30px;font-weight:900;letter-spacing:.05em;}
  .mm-card .d{font-size:14px;color:#aeb4d8;}
  .mm-card.single{border-color:#ffd76b;box-shadow:0 0 40px #ffd76b44;}
  .mm-card.single:hover{box-shadow:0 0 60px #ffd76b88;}
  .mm-card.dual{border-color:#2b7bff;box-shadow:0 0 40px #2b7bff44;}
  .mm-card.dual:hover{box-shadow:0 0 60px #2b7bff88;}
  .mm-title{position:absolute;top:14%;left:0;right:0;text-align:center;color:#fff;font-size:26px;font-weight:800;letter-spacing:.1em;}
  .mm-x{position:absolute;top:18px;right:22px;background:transparent;border:none;color:#fff;font-size:26px;cursor:pointer;}`;
  document.head.appendChild(style);

  const back = document.createElement('div');
  back.className = 'mm-back';
  back.innerHTML = `
    <div class="mm-title">選擇模式</div>
    <button class="mm-x">✕</button>
    <div class="mm-card single" data-mode="single"><div class="ic">🙋</div><div class="t">單人</div><div class="d">一人雙手 · 挑戰評級</div></div>
    <div class="mm-card dual" data-mode="dual"><div class="ic">🤺</div><div class="t">雙人</div><div class="d">左右對戰 · 比分數</div></div>`;
  hud.appendChild(back);

  const close = () => { back.style.display = 'none'; };
  back.querySelector('.mm-x').addEventListener('click', close);
  back.addEventListener('click', (e) => { if (e.target === back) close(); });
  back.querySelectorAll('.mm-card').forEach((c) =>
    c.addEventListener('click', () => { close(); onPick(c.dataset.mode); }));

  return {
    show() { back.style.display = 'flex'; },
    hide: close,
  };
}
```

- [ ] **Step 2: 建置** — Run: `npm run build` → 無錯。

- [ ] **Step 3: Commit**
```bash
git add src/mode-modal.js
git commit -m "feat(mode): 模式選擇彈窗 單人/雙人 霓虹卡片"
```

---

### Task 7: lights.js — 試聽演唱會體積光引擎

**Files:** Create `src/lights.js`

- [ ] **Step 1: 建立檔案**
```js
// 選歌試聽的體積光：canvas(mix-blend screen)。光束+浮塵粒子(被光照到變亮)+光落點。
export function createLights(hud) {
  const lc = document.createElement('canvas');
  lc.style.cssText = 'position:absolute;inset:0;z-index:2;pointer-events:none;mix-blend-mode:screen;';
  hud.appendChild(lc);
  const lx = lc.getContext('2d');
  const resize = () => { lc.width = innerWidth * devicePixelRatio; lc.height = innerHeight * devicePixelRatio; };
  resize(); addEventListener('resize', resize);

  const BEAMS = [
    { x: 0.12, c: [110, 175, 255] }, { x: 0.31, c: [170, 110, 255] }, { x: 0.5, c: [255, 120, 210] },
    { x: 0.69, c: [110, 175, 255] }, { x: 0.88, c: [170, 110, 255] },
  ];
  const DUST = Array.from({ length: 150 }, (_, k) => ({ x: (k * 0.137) % 1, y: (k * 0.311) % 1, z: 0.4 + (k % 6) / 10, ph: k }));
  let t = 0, inten = 0, playing = false;
  const ang = (i) => Math.sin(t * 0.6 + i * 1.3) * 0.28;

  function frame() {
    t += 0.016; inten += ((playing ? 1 : 0) - inten) * 0.05;
    const W = lc.width, H = lc.height;
    lx.clearRect(0, 0, W, H);
    if (inten > 0.01) {
      lx.globalCompositeOperation = 'lighter';
      BEAMS.forEach((b, i) => {
        const ax = b.x * W, a = ang(i), topW = 0.02 * W, botW = 0.14 * W, len = 1.25 * H;
        lx.save(); lx.translate(ax, -0.05 * H); lx.rotate(a);
        const g = lx.createLinearGradient(0, 0, 0, len);
        g.addColorStop(0, `rgba(${b.c[0]},${b.c[1]},${b.c[2]},${0.45 * inten})`);
        g.addColorStop(1, 'rgba(0,0,0,0)');
        lx.fillStyle = g; lx.filter = 'blur(14px)';
        lx.beginPath(); lx.moveTo(-topW, 0); lx.lineTo(topW, 0); lx.lineTo(botW, len); lx.lineTo(-botW, len); lx.closePath(); lx.fill();
        lx.restore();
        const bx = ax + Math.sin(a) * len;
        const pg = lx.createRadialGradient(bx, H * 0.85, 0, bx, H * 0.85, 0.22 * H);
        pg.addColorStop(0, `rgba(${b.c[0]},${b.c[1]},${b.c[2]},${0.3 * inten})`);
        pg.addColorStop(1, 'rgba(0,0,0,0)');
        lx.filter = 'none'; lx.fillStyle = pg; lx.fillRect(0, 0, W, H);
      });
      DUST.forEach((d) => {
        d.y += 0.0006 / d.z; d.x += Math.sin(t * 0.5 + d.ph) * 0.0002; if (d.y > 1) d.y = 0;
        const px = d.x * W, py = d.y * H;
        let bright = 0.04;
        BEAMS.forEach((b, i) => { const axisX = b.x * W + Math.tan(ang(i)) * py; bright += Math.max(0, 1 - Math.abs(px - axisX) / (0.09 * W)) * 0.9; });
        lx.fillStyle = `rgba(200,225,255,${Math.min(1, bright) * inten})`;
        lx.beginPath(); lx.arc(px, py, (1.5 + d.z * 1.5) * devicePixelRatio, 0, 7); lx.fill();
      });
    }
    requestAnimationFrame(frame);
  }
  frame();
  return { setPlaying(v) { playing = v; } };
}
```

- [ ] **Step 2: 建置** — Run: `npm run build` → 無錯。

- [ ] **Step 3: Commit**
```bash
git add src/lights.js
git commit -m "feat(lights): 試聽演唱會體積光引擎(光束+粒子+光落點)"
```

---

### Task 8: select-screen.js — 霓虹背景 + 試聽體積光 + 音波置底不重疊

**Files:** Modify `src/select-screen.js`；Create `public/select-bg.png`（複製 mockups/assets/select-bg-1.png）

- [ ] **Step 1: 放背景圖進 public**
```bash
cp "mockups/assets/select-bg-1.png" "public/select-bg.png"
```

- [ ] **Step 2: 背景與體積光接入**

在 `select-screen.js` 頂部 import 區加：
```js
import { createLights } from './lights.js';
```
在 `injectStyle()` 的 `.ss-screen{...}` 規則中，把 `background:radial-gradient(...)...#0b0b12` 換成同時鋪底圖：
```css
  .ss-screen{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;
    gap:0;background:#05060d url('/select-bg.png') center/cover no-repeat;pointer-events:auto;color:#fff;
    font-family:system-ui,"Segoe UI",sans-serif;z-index:5;}`;
```
（GitHub Pages base 由 Vite 處理；`public/` 檔以 `import.meta.env.BASE_URL` 引用較穩，改用行內背景：在 `createSelectScreen` 內建立後設定 `screen.style.backgroundImage = \`url("${import.meta.env.BASE_URL}select-bg.png")\`;`，並把上面 CSS 的 background 只留底色 `#05060d`。）

在 `createSelectScreen` 內、建立 `screen` 後加：
```js
  screen.style.backgroundImage = `url("${import.meta.env.BASE_URL}select-bg.png")`;
  screen.style.backgroundSize = 'cover';
  screen.style.backgroundPosition = 'center';
  const lights = createLights(screen);
```
在 `startPreview()` 結尾加 `lights.setPlaying(true);`，在 `stopPreview()` 結尾加 `lights.setPlaying(false);`。

- [ ] **Step 3: 音波置底限高不重疊**

把 `.ss-eq{...height:15vh...}` 改為 `height:11vh`，確認 `.ss-top,.ss-stage,.ss-meta,.ss-controls,.ss-dots{position:relative;z-index:1;}` 存在（已存在）。體積光 canvas z-index:2 在 .ss-eq(z0) 之上、內容(z1)... 需讓內容在光之上：把該行 z-index 由 1 改為 3。

- [ ] **Step 4: 建置與手動驗證** — Run: `npm run build`（無錯）；`npm run dev` 開頁，選歌畫面有霓虹背景、點試聽有光束從上打下+粒子、音波在最底不擋按鈕。

- [ ] **Step 5: Commit**
```bash
git add src/select-screen.js public/select-bg.png
git commit -m "feat(select): 霓虹背景+試聽體積光+音波置底不重疊"
```

---

### Task 9: ui.js — 導引圓固定/雙圓、遊戲化能量管、combo爆發、單人HUD、結算、校名

**Files:** Modify `src/ui.js`

- [ ] **Step 1: 校名 + combo 追蹤變數**

在 `createUI` 內、`let guidePhase = 0;` 後加：
```js
  const SCHOOL = '🏫 澎湖縣湖西鄉龍門國民小學 · 畫圈對決';
  const bursts = []; // combo 爆發粒子/文字
  let prevCombo = { A: 1, B: 1, S: 1 };
  function drawSchool() {
    ctx.fillStyle = '#dfe6ff'; ctx.font = `${Math.round(canvas.height * 0.022)}px system-ui`;
    ctx.textAlign = 'left'; ctx.textBaseline = 'top';
    ctx.fillText(SCHOOL, canvas.width * 0.012, canvas.height * 0.02);
  }
  function triggerBurst(cx, cy, color, mult) {
    bursts.push({ x: cx, y: cy, color, mult, life: 1 });
    for (let i = 0; i < 16; i++) { const a = (i / 16) * Math.PI * 2; particles.push({ x: cx, y: cy, vx: Math.cos(a) * 9, vy: Math.sin(a) * 9, life: 1, color }); }
  }
  function drawBursts() {
    for (let i = bursts.length - 1; i >= 0; i--) {
      const b = bursts[i]; b.life -= 0.02; b.y -= 2;
      if (b.life <= 0) { bursts.splice(i, 1); continue; }
      ctx.globalAlpha = b.life; ctx.fillStyle = b.color;
      ctx.font = `900 ${Math.round(canvas.height * 0.08)}px system-ui`;
      ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
      ctx.fillText(`COMBO x${b.mult}!`, b.x, b.y);
      ctx.globalAlpha = 1;
    }
  }
  function flash(color) {
    ctx.save(); ctx.globalAlpha = 0.25; ctx.fillStyle = color;
    ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.restore();
  }
```
（`onBurst` 回呼供 main 觸發音效：在 return 物件加 `onComboBurst: null`，triggerBurst 內若 `api.onComboBurst` 存在則呼叫——見 Step 5。）

- [ ] **Step 2: 遊戲化能量管 drawTube（取代舊 drawBar 用法，新增函式）**

在 `drawBar` 函式後新增：
```js
  // 遊戲化能量管：外框圓角、分節發光填充、頭像、分數、combo
  function drawTube(xFrac, score, comboMult, color, label, icon) {
    const W = canvas.width, H = canvas.height;
    const bw = W * 0.035, bh = H * 0.6, x = xFrac * W - bw / 2, y = H * 0.2;
    // 頭像
    ctx.fillStyle = color; ctx.font = `${Math.round(H * 0.05)}px system-ui`;
    ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
    ctx.fillText(icon, xFrac * W, y - H * 0.05);
    // 管
    ctx.strokeStyle = color + 'aa'; ctx.lineWidth = 3;
    roundRectPath(x, y, bw, bh, bw / 2); ctx.stroke();
    const frac = Math.max(0, Math.min(1, score / 3000));
    ctx.save(); roundRectPath(x, y, bw, bh, bw / 2); ctx.clip();
    // 分節發光
    const cells = 12, gap = bh * 0.012, ch = (bh - gap * (cells - 1)) / cells;
    const lit = Math.round(frac * cells);
    for (let i = 0; i < lit; i++) {
      ctx.fillStyle = color; ctx.shadowColor = color; ctx.shadowBlur = 12;
      const cy = y + bh - (i + 1) * ch - i * gap;
      ctx.fillRect(x, cy, bw, ch);
    }
    ctx.restore(); ctx.shadowBlur = 0;
    // 分數 + combo
    ctx.fillStyle = color; ctx.font = `900 ${Math.round(H * 0.03)}px system-ui`;
    ctx.fillText(`${label} ${Math.round(score)}`, xFrac * W, y + bh + H * 0.04);
    ctx.fillStyle = '#ffd76b'; ctx.font = `900 ${Math.round(H * 0.035)}px system-ui`;
    ctx.fillText(`🔥x${comboMult}`, xFrac * W, y + bh + H * 0.09);
  }
  function roundRectPath(x, y, w, h, r) {
    r = Math.min(r, w / 2, h / 2);
    ctx.beginPath(); ctx.moveTo(x + r, y);
    ctx.arcTo(x + w, y, x + w, y + h, r); ctx.arcTo(x + w, y + h, x, y + h, r);
    ctx.arcTo(x, y + h, x, y, r); ctx.arcTo(x, y, x + w, y, r); ctx.closePath();
  }
```

- [ ] **Step 3: 取代 render（支援 dual/single、固定導引圓、combo 爆發、隱藏攝影機由 main 控制）**

把 return 物件裡的 `render(state)` 整個換成：
```js
    // 共用：頂部段落 + 校名 + 段落大字 + 導引圓 + 手 + 能量管 + combo 爆發
    render(state) {
      const W = canvas.width, H = canvas.height;
      ctx.clearRect(0, 0, W, H);
      drawDivider();
      drawSchool();
      const glyph = state.segDir === 'F' ? '↻ 正轉' : state.segDir === 'R' ? '↺ 反轉' : '⏸ 休息';
      centerText(glyph, 0.12, 0.08, '#fff');
      if (state.nextDir) {
        const nn = state.nextDir === 'F' ? '正轉' : state.nextDir === 'R' ? '反轉' : '休息';
        centerText(`下一個：${nn}  ${Math.ceil(state.nextIn)}`, 0.22, 0.03, '#cdd6ff');
      }
      centerText(`⏱ ${Math.ceil(state.timeLeft)}s`, 0.05, 0.03, '#fff');

      const R = Math.min(W, H) * 0.16;
      guidePhase = (guidePhase + (state.guideOmega || 0) / 60) % (Math.PI * 2);

      if (state.mode === 'single') {
        drawTube(0.5, state.score, state.comboMult, colorA, 'YOU', '🕹️');
        // 兩個導引圓 + 兩手
        if (state.segDir !== 'S') {
          drawGuide({ x: W * 0.35, y: H * 0.5 }, R, state.segDir, colorA);
          drawGuide({ x: W * 0.65, y: H * 0.5 }, R, state.segDir, colorB);
        }
        drawHand(state.handL, colorA); drawHand(state.handR, colorB);
        // combo 爆發偵測
        if (state.comboMult > prevCombo.S) { triggerBurst(W * 0.5, H * 0.4, colorA, state.comboMult); flash(colorA); if (api.onComboBurst) api.onComboBurst(state.comboMult); }
        prevCombo.S = state.comboMult;
      } else {
        drawTube(0.06, state.A.score, state.A.comboMult, colorA, 'A', '🕹️');
        drawTube(0.94, state.B.score, state.B.comboMult, colorB, 'B', '🎮');
        for (const [side, color, cx] of [['A', colorA, 0.25], ['B', colorB, 0.75]]) {
          if (state.segDir !== 'S') drawGuide({ x: cx * W, y: H * 0.5 }, R, state.segDir, color);
          drawHand(state[side].hand, color);
          if (state[side].comboMult > prevCombo[side]) { triggerBurst(cx * W, H * 0.4, color, state[side].comboMult); flash(color); if (api.onComboBurst) api.onComboBurst(state[side].comboMult); }
          prevCombo[side] = state[side].comboMult;
        }
      }
      // 粒子 + combo 文字
      for (let i = particles.length - 1; i >= 0; i--) {
        const p = particles[i]; p.x += p.vx; p.y += p.vy; p.vx *= 0.96; p.vy *= 0.96; p.life -= 0.03;
        if (p.life <= 0) { particles.splice(i, 1); continue; }
        ctx.globalAlpha = p.life; ctx.fillStyle = p.color;
        ctx.beginPath(); ctx.arc(p.x, p.y, 5, 0, Math.PI * 2); ctx.fill();
      }
      ctx.globalAlpha = 1;
      drawBursts();
    },
```

- [ ] **Step 4: 取代 victory（dual/single 結算）**

把 `victory(who, scoreA, scoreB)` 換成：
```js
    victory(result) {
      // result: {mode:'dual', who, scoreA, scoreB} 或 {mode:'single', score, grade}
      ctx.fillStyle = '#000c'; ctx.fillRect(0, 0, canvas.width, canvas.height);
      ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
      const cx = canvas.width / 2, H = canvas.height;
      if (result.mode === 'single') {
        ctx.fillStyle = '#ffd76b'; ctx.font = `900 ${Math.round(H * 0.16)}px system-ui`;
        ctx.fillText(result.grade, cx, H * 0.4);
        ctx.fillStyle = '#fff'; ctx.font = `${Math.round(H * 0.05)}px system-ui`;
        ctx.fillText(`分數 ${Math.round(result.score)}`, cx, H * 0.58);
      } else {
        ctx.fillStyle = result.who === 'A' ? colorA : result.who === 'B' ? colorB : '#fff';
        ctx.font = `900 ${Math.round(H * 0.1)}px system-ui`;
        ctx.fillText(result.who ? `玩家 ${result.who} 勝利！` : '平手！', cx, H * 0.4);
        ctx.fillStyle = '#fff'; ctx.font = `${Math.round(H * 0.05)}px system-ui`;
        ctx.fillText(`A ${Math.round(result.scoreA)} : ${Math.round(result.scoreB)} B`, cx, H * 0.56);
      }
      ctx.fillStyle = '#aeb4d8'; ctx.font = `${Math.round(H * 0.025)}px system-ui`;
      ctx.fillText(SCHOOL, cx, H * 0.7);
    },
```

- [ ] **Step 5: 在 return 物件加 `onComboBurst` 欄位並用 `api` 參照**

把 `return {` 改成 `const api = {`，並在函式最後 `return api;`。（即整個回傳物件命名為 `api`，讓 render 內可呼叫 `api.onComboBurst`。）在 `api` 內加一行：`onComboBurst: null,`。

- [ ] **Step 6: 建置** — Run: `npm run build` → 無錯。

- [ ] **Step 7: Commit**
```bash
git add src/ui.js
git commit -m "feat(ui): 固定/雙導引圓+遊戲化能量管+combo爆發+單人HUD+結算+校名"
```

---

### Task 10: main.js — 模式流程 + 單/雙人分支 + 隱藏攝影機

**Files:** Modify `src/main.js`

- [ ] **Step 1: import 與狀態**

import 區加：
```js
import { scoreStepSingle, gradeFor } from './score.js';
import { createModeModal } from './mode-modal.js';
```
狀態區加：
```js
let mode = 'dual';
let scoreS = { score: 0, combo: 0 };
const rotL = { lastAngle: null };
const rotR = { lastAngle: null };
let modeModal = null;
```

- [ ] **Step 2: 開始遊戲 → 模式彈窗 → 準備**

把 `createSelectScreen(hud, (idx, m) => {...})` 的回呼改成先開模式彈窗：
```js
  selectScreen = createSelectScreen(hud, (idx, m) => {
    selectedIdx = idx; lenMode = m;
    modeModal.show();
  });
  modeModal = createModeModal(hud, (picked) => {
    mode = picked;
    selectScreen.hide(); showControls(false);
    startReady();
  });
```
`api.onComboBurst` 接音效：在 `const ui = createUI(canvas);` 後加 `ui.onComboBurst = (tier) => { import('./sfx.js').then(m => m.sfx.comboBurst(tier)); };`（或於頂部 import sfx 後直接 `ui.onComboBurst = (t)=>sfx.comboBurst(t);`）。採後者：頂部加 `import { sfx } from './sfx.js';` 並 `ui.onComboBurst = (t) => sfx.comboBurst(t);`。

- [ ] **Step 3: startPlaying 依模式 + 隱藏攝影機**

把 `startPlaying()` 內 `media.playTrack(selectedIdx);` 後加隱藏攝影機、重置單人分數：
```js
  scoreS = { score: 0, combo: 0 };
  rotL.lastAngle = null; rotR.lastAngle = null;
  video.style.opacity = '0'; // 開打隱藏攝影機，只看 MV + 手
```
在回到選歌處（victory timeout 內）與 startReady 內加還原：`video.style.opacity = '';`（讓準備階段看得到攝影機）。準備階段本來就顯示，所以在 `startReady()` 開頭加 `video.style.opacity = '';`。

- [ ] **Step 4: loop 依模式分流**

在 `loop` 內，把 `phase === 'ready'` 與 `phase === 'playing'` 兩段依 mode 分流。準備段：單人用中央一個方塊（沿用 boxHit 但單人只需任一手），雙人維持。簡化：ready 段兩模式都用現有雙框邏輯，但單人只需 A 框就緒即可 —— 改為：
```js
  } else if (phase === 'ready') {
    if (mode === 'single') {
      const hit = ui.boxHit(handA, 'A') || ui.boxHit(handB, 'A');
      readyState.A = { ...updateHold(readyState.A.hold, hit, dt, READY_NEED) };
      ui.drawReady({ need: READY_NEED, A: { hand: handA || handB, hold: readyState.A.hold, ready: readyState.A.ready }, B: { hand: null, hold: 0, ready: true } });
      if (readyState.A.ready) startPlaying();
    } else {
      const hitA = ui.boxHit(handA, 'A'), hitB = ui.boxHit(handB, 'B');
      readyState.A = { ...updateHold(readyState.A.hold, hitA, dt, READY_NEED) };
      readyState.B = { ...updateHold(readyState.B.hold, hitB, dt, READY_NEED) };
      ui.drawReady({ need: READY_NEED, A: { hand: handA, hold: readyState.A.hold, ready: readyState.A.ready }, B: { hand: handB, hold: readyState.B.hold, ready: readyState.B.ready } });
      if (readyState.A.ready && readyState.B.ready) startPlaying();
    }
    sendStop();
  }
```
playing 段改成依 mode。先在 loop 前段依 mode 取得手/omega：雙人維持現有 armA/armB（split）；單人改用 `pose.readFull()`。因 `pose.read()` 與 `readFull()` 不同，於 loop 開頭依 mode 擇一。把 loop 開頭的 `const { A, B } = await pose.read();` 起的偵測區塊改為：
```js
  let handA = null, handB = null, handL = null, handR = null;
  let omegaA = 0, omegaB = 0, omegaL = 0, omegaR = 0;
  let armA = null, armB = null;
  if (mode === 'single' && phase !== 'select') {
    const f = await pose.readFull();
    const pickL = f && f.leftWrist && f.leftShoulder && f.leftWrist.score > 0.3 ? { wrist: f.leftWrist, shoulder: f.leftShoulder } : null;
    const pickR = f && f.rightWrist && f.rightShoulder && f.rightWrist.score > 0.3 ? { wrist: f.rightWrist, shoulder: f.rightShoulder } : null;
    if (pickL) { const ang = wristAngle(pickL.wrist, pickL.shoulder); const r = trackRotation(rotL, ang, dt); rotL.lastAngle = r.state.lastAngle; omegaL = r.omega; handL = toCanvasFull(pickL.wrist); } else rotL.lastAngle = null;
    if (pickR) { const ang = wristAngle(pickR.wrist, pickR.shoulder); const r = trackRotation(rotR, ang, dt); rotR.lastAngle = r.state.lastAngle; omegaR = r.omega; handR = toCanvasFull(pickR.wrist); } else rotR.lastAngle = null;
    // 準備階段單人用 handA/handB 當任一手
    handA = handL; handB = handR;
  } else {
    const { A, B } = await pose.read();
    armA = pickArm(A); armB = pickArm(B);
    if (armA) { const ang = wristAngle(armA.wrist, armA.shoulder); const r = trackRotation(rotA, ang, dt); rotA.lastAngle = r.state.lastAngle; omegaA = r.omega; handA = toCanvas(armA.wrist, 'A'); } else rotA.lastAngle = null;
    if (armB) { const ang = wristAngle(armB.wrist, armB.shoulder); const r = trackRotation(rotB, ang, dt); rotB.lastAngle = r.state.lastAngle; omegaB = r.omega; handB = toCanvas(armB.wrist, 'B'); } else rotB.lastAngle = null;
  }
```
新增全畫面座標轉換（鏡像、無左右半偏移）：
```js
function toCanvasFull(pt) {
  const fx = video.videoWidth - pt.x; // 鏡像
  return { x: (fx / video.videoWidth) * canvas.width, y: (pt.y / video.videoHeight) * canvas.height };
}
```
playing 段：
```js
  } else if (phase === 'playing') {
    const elapsed = (performance.now() - startTime) / 1000;
    const timeLeft = Math.max(0, roundSec - elapsed);
    const { current, next, remain } = segmentAt(chart, elapsed);
    const segDir = current ? current.dir : 'S';
    const guideOmega = targetOmegaFor(bpm, SCORE_CFG);

    if (mode === 'single') {
      scoreS = scoreStepSingle(scoreS, segDir, omegaL, omegaR, dt, bpm, SCORE_CFG);
      const fa = fanCommand(omegaL, CONFIG), fb = fanCommand(omegaR, CONFIG);
      sender.send(formatCommand(
        { ...fa, energy: Math.min(100, scoreS.score / SCORE_CFG.scoreForFullBar * 100) },
        { ...fb, energy: Math.min(100, scoreS.score / SCORE_CFG.scoreForFullBar * 100) })).catch(() => {});
      ui.render({ mode: 'single', timeLeft, segDir, nextDir: next ? next.dir : null, nextIn: remain, guideOmega,
        score: scoreS.score, comboMult: comboMultiplier(scoreS.combo, SCORE_CFG), handL, handR });
      if (!ended && elapsed >= roundSec) {
        ended = true; phase = 'victory'; sendStop(); mvVideo.pause();
        ui.victory({ mode: 'single', score: scoreS.score, grade: gradeFor(scoreS.score, roundSec, SCORE_CFG) });
        setTimeout(() => { selectScreen.show(media.tracks); showControls(true); video.style.opacity = ''; phase = 'select'; }, 5000);
      }
    } else {
      scoreA = scoreStep(scoreA, segDir, omegaA, dt, bpm, SCORE_CFG);
      scoreB = scoreStep(scoreB, segDir, omegaB, dt, bpm, SCORE_CFG);
      const fa = fanCommand(omegaA, CONFIG), fb = fanCommand(omegaB, CONFIG);
      sender.send(formatCommand(
        { ...fa, energy: Math.min(100, scoreA.score / SCORE_CFG.scoreForFullBar * 100) },
        { ...fb, energy: Math.min(100, scoreB.score / SCORE_CFG.scoreForFullBar * 100) })).catch(() => {});
      ui.render({ mode: 'dual', timeLeft, segDir, nextDir: next ? next.dir : null, nextIn: remain, guideOmega,
        A: { score: scoreA.score, comboMult: comboMultiplier(scoreA.combo, SCORE_CFG), hand: handA },
        B: { score: scoreB.score, comboMult: comboMultiplier(scoreB.combo, SCORE_CFG), hand: handB } });
      if (!ended && elapsed >= roundSec) {
        ended = true; phase = 'victory'; sendStop(); mvVideo.pause();
        ui.victory({ mode: 'dual', who: higherScore(scoreA.score, scoreB.score), scoreA: scoreA.score, scoreB: scoreB.score });
        setTimeout(() => { selectScreen.show(media.tracks); showControls(true); video.style.opacity = ''; phase = 'select'; }, 5000);
      }
    }
  }
```
（`select` 段維持 `ui.clear(); sendStop();`。移除舊 victory 呼叫殘留。確保 `mvVideo.pause()` 停音樂。）

- [ ] **Step 2 補：** 頂部 `import { sfx } from './sfx.js';` 後、`const ui = createUI(canvas);` 之後加 `ui.onComboBurst = (t) => sfx.comboBurst(t);`。

- [ ] **Step 5: 建置與手動驗證（sim）** — Run: `npm test`(全綠)、`npm run build`(無錯)、`npm run dev`：
  - LOADING 標題有校名 → 選歌(霓虹背景/試聽體積光/音波置底) → 開始遊戲跳**模式彈窗** → 選單人：中央一框 5 秒 → 開打**攝影機消失只剩 MV+雙手亮點+兩導引圓+中央能量管**，兩手同方向畫圈加分、combo 爆發閃光+音效，時間到顯示**分數+評級**；選雙人：兩框 → 左右對戰、combo 爆發、時間到比分。左上校名常駐。

- [ ] **Step 6: Commit**
```bash
git add src/main.js
git commit -m "feat(main): 模式彈窗+單/雙人分支+開打隱藏攝影機+停音樂結算"
```

---

## Self-Review 檢查結果

**Spec 覆蓋：** 模式彈窗(T6+T10)、單人計分/評級(T1)、單人偵測(T4)、單人HUD/雙導引圓/結算(T9+T10)、8秒段落(T2)、隱藏攝影機(T10)、combo爆發+音效(T3+T9+T10)、遊戲化能量管(T9)、導引圓固定(T9)、校名(T5+T9)、停音樂+結算(T10)、選歌霓虹背景+體積光+音波不重疊(T7+T8)、LOADING校名(T5)。

**型別一致：** `scoreStepSingle`/`gradeFor`/`SCORE_CFG.grade*`、`readFull`、`createModeModal(onPick)`、`createLights().setPlaying`、`ui.render({mode,...})`/`ui.victory({mode,...})`/`ui.onComboBurst`、`toCanvasFull`、`comboMultiplier`/`targetOmegaFor` 於各 Task 一致。

**佔位符：** 無 TBD；程式步驟均含完整程式碼。可調值(評級門檻/段長/光參數/combo音)為校正旋鈕。
